version: '3'
services:
  api:
    build: .
    ports:
      - '8001:8001'
      - '9001:9001'
    volumes:
      - .:/home/node/app
    entrypoint: 'npm run dev'
    networks:
      - facile
    depends_on:
      - db
  db:
    image: mongo
    restart: always
    container_name: db
    volumes:
      - ./data-node:/data/db
    ports:
      - 27017:27017
    command: mongod --noauth --smallfiles
    networks:
      - facile
networks:
  facile:
    external:
      name: facile
